"""Pure CFFBPS equation functions.

Each function computes one part of the fire-behavior model from explicit inputs and
returns new values; none read or mutate ``FBP`` instance state. The ``FBP`` facade
orchestrates them.
"""
